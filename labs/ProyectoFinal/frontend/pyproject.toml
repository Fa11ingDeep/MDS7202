[project]
name = "chaucherapp-frontend"
version = "0.1.0"
description = "Frontend Gradio para priorización de tickets ChaucherApp"
requires-python = ">=3.12"
dependencies = [
    "gradio",
    "requests",
    "python-dotenv",
]

[tool.uv]
package = false
